{
  "name": "img-merge-pdf",
  "version": "1.0.0",
  "description": "Merges multiple images into a single PDF on the client side",
  "scripts": {
    "build": "browserify src/js/* -o src/js/bundle.js",
    "uglify": "browserify src/js/* | uglifyjs -c -m > src/js/bundle.js"
  },
  "author": "Dom Slee",
  "license": "MIT",
  "dependencies": {
    "image-compressor": "^1.2.0",
    "pdfkit": "^0.8.3",
    "sortablejs": "^1.7.0",
    "uglify": "^0.1.5"
  }
}
